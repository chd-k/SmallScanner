<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".ui.fragments.PhotoFragment">

    <ImageView
        android:id="@+id/iv_photo_products"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginBottom="8dp"
        android:scaleType="fitCenter"
        app:layout_constraintBottom_toTopOf="@+id/btn_photo_gallery"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:contentDescription="@string/descr_iv_photo_products" />

    <Button
        android:id="@+id/btn_photo_gallery"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/text_btn_photo_gallery"
        android:textSize="14sp"
        app:icon="@drawable/vector_folder"
        app:iconSize="24dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/btn_photo_camera"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/btn_photo_camera"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="16dp"
        android:text="@string/text_btn_photo_camera"
        android:textSize="14sp"
        app:icon="@drawable/vector_camera"
        app:iconSize="24dp"
        app:layout_constraintBottom_toBottomOf="@+id/btn_photo_gallery"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/btn_photo_gallery"
        app:layout_constraintTop_toTopOf="@+id/btn_photo_gallery" />

</androidx.constraintlayout.widget.ConstraintLayout>